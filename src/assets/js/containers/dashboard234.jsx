import React, { useCallback, useState, useMemo } from 'react';
import axios from 'axios';
import * as d3 from 'd3';
import range from 'd3-array';

function Dashboard() {
    //generate date on first load
  const data = useMemo(() => {
    
  }, []);
  return (
    <div>Dashboard</div>
  );
}

export default Dashboard;
